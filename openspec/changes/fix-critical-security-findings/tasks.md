## 1. Secret key hardening

- [x] 1.1 Generate a new random Django secret key (distinct from the currently-committed value) and replace the hardcoded `SECRET_KEY` in `newfies/newfies_dialer/settings.py` with `os.environ.get('DJANGO_SECRET_KEY', '<new-dev-only-fallback>')`; verify by starting the app with no env var set and confirming it still boots with the fallback, then with `DJANGO_SECRET_KEY` set and confirming `django.conf.settings.SECRET_KEY` reflects it (e.g. via `django-admin shell -c "from django.conf import settings; print(settings.SECRET_KEY)"`). [Verified via syntax check and code review only — this Django 1.7.7/Python 2 app cannot be booted in this sandbox (no Python 2 runtime available); actual boot verification should be done in CI/staging.]
- [x] 1.2 Add a documented `SECRET_KEY = '...'` placeholder line with a generation instruction to `install/conf/settings_local.py`; verify by reviewing the diff shows the new line without disturbing existing template settings.
- [x] 1.3 Add a comment above `DATABASES['default']` in `newfies/newfies_dialer/settings.py` clarifying that sqlite3 requires no username/password; verify by reading the comment renders directly above the empty `USER`/`PASSWORD` fields.

## 2. Parameterize multi-delete/count query paths

- [x] 2.1 Rewrite `get_contact_count` and the multi-delete branch(es) in `newfies/dialer_contact/views.py` (`phonebook_del`, `contact_del_list`) to validate `getlist()` values as integers and use `.filter(...__in=ids)` instead of `.extra(where=...)`; verify by running/adding a test that submits a non-numeric ID alongside valid ones and asserts only the valid IDs are affected and no exception/500 occurs. [Rewrite done for all 3 sites; test run pending in section 5.]
- [x] 2.2 Rewrite the multi-delete branch in `newfies/mod_sms/views.py` (SMS campaign delete/stop) the same way; verify with an equivalent test for SMS campaign multi-delete. [Test run pending in section 5.]
- [x] 2.3 Rewrite the multi-delete branch in `newfies/survey/views.py` the same way; verify with an equivalent test for survey multi-delete. [Test run pending in section 5.]
- [x] 2.4 Manually re-check each rewritten endpoint still enforces its existing `request.user`/permission scoping (the `__in` filter must keep the same `.filter(user=..., ...)` / `phonebook__user=...` conditions as before); verify by diffing the queryset chain before/after for each of the 6 sites. [Confirmed: `get_contact_count` and `phonebook_del`'s contact branch keep `phonebook__user=request.user`; `phonebook_del`'s phonebook branch and `survey`'s multi-delete keep `user=request.user`. `dialer_contact.contact_del`'s multi-delete branch and `mod_sms.sms_campaign_del`'s multi-delete branch had NO user-scoping before this change either — that is a pre-existing authorization gap, out of scope here (not one of the selected Sonar findings); preserved exactly, not introduced or fixed.]

## 3. Parameterize raw SQL execution

- [x] 3.1 Convert `newfies/dialer_contact/tasks.py`'s `importcontact_custom_sql` to build its `INSERT ... SELECT` SQL with `%s` placeholders and pass `campaign_id`/`phonebook_id`/`limit_import` via `cursor.execute(sql, params)`; verify by running the existing contact-import test/task path and confirming subscriber rows are still created as before. [`limit_import` kept as literal SQL text per design.md Decision 3 — it's an internally-built fragment ('LIMIT <int>'/'LIMIT 0'/''), not a bindable scalar value; campaign_id/phonebook_id are now parameters. Test run pending in section 5.]
- [x] 3.2 Convert `newfies/mod_sms/tasks.py`'s `importcontact_custom_sql` (both MySQL and PostgreSQL branches) the same way; verify by running the existing SMS contact-import task path. [Test run pending in section 5.]
- [x] 3.3 Convert `newfies/mod_sms/models.py`'s raw query (contacts not yet subscribed) to `Contact.objects.raw(query, params)` with `%s` placeholders instead of string-formatted `self.id`; verify by exercising the method (or its existing test coverage) and confirming the returned contact list is unchanged. [Test run pending in section 5.]

## 4. Resolve remaining hotspots via review, not code change

- [x] 4.1 Add a one-line comment at each `.extra(select=...)` site in `newfies/frontend/views.py` and `newfies/mod_sms/views.py` stating `date_length`/the select expression is built from a fixed internal constant, not request input; verify by reading the comment sits directly above each `.extra(select=...)` call. [Comment added above the shared `select_data` dict definition in each file (both of that file's two `.extra(select=select_data)` calls reuse it).]
- [x] 4.2 In SonarQube, mark the corresponding SQL-injection hotspots (frontend/views.py:194, frontend/views.py:223, mod_sms/views.py:422, mod_sms/views.py:448) and the sqlite3 DB-password vulnerability (settings.py:40) as reviewed/"Safe" with a short justification referencing this change; verify by reloading the project dashboard and confirming these no longer appear as TO_REVIEW/open. [Done via API: 4 hotspots marked REVIEWED/SAFE, settings.py:40 issue resolved as false-positive, each with a comment referencing this change. Corrected an earlier miscount in proposal.md/design.md: 9 of the 13 SQL-injection hotspots are code-fixed and 4 (not 6) are date_length-only, resolved here by review; the DB-password item is a separate BLOCKER vulnerability, not one of the 13.]

## 5. Verification

- [ ] 5.1 Re-run the project's Python test suite and confirm no regressions in contact, phonebook, SMS campaign, and survey delete/count flows; verify via the test runner's pass/fail output.
- [ ] 5.2 Trigger a SonarQube re-analysis and confirm: 0 BLOCKER vulnerabilities, and the 9 code-fixed SQL-injection hotspots (dialer_contact/tasks.py:116, dialer_contact/views.py:100/138/145/359, mod_sms/models.py:306, mod_sms/tasks.py:357, mod_sms/views.py:228, survey/views.py:151) no longer appear as open HIGH hotspots, alongside the 4 date_length hotspots and the settings.py:40 issue already resolved via review in task 4.2; verify by comparing the new dashboard counts against the baseline captured in this change's proposal.
