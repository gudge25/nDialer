## 1. Settings flag

- [x] 1.1 Add `ALLOW_SELF_REGISTRATION = False` to `newfies/newfies_dialer/settings.py` and verify the app still starts with the default settings module (no env override needed).
- [x] 1.2 Add `ALLOW_SELF_REGISTRATION = os.environ.get('ALLOW_SELF_REGISTRATION', 'false').lower() == 'true'` to `docker/settings_docker.py`, next to the existing `DEBUG` line, and verify it overrides the base default when the env var is set.
- [x] 1.3 Add `ALLOW_SELF_REGISTRATION=false` to `.env.example` next to `CREATE_DEFAULT_SUPERUSER`, with a one-line comment explaining what it gates.

## 2. URL routing

- [x] 2.1 In `newfies/newfies_dialer/urls.py`, replace the `(r'^accounts/', include('registration.backends.default.urls'))` line with a local urlpattern list that mounts only `login`, `logout`, `password_change`, and `password_change_done` from `django.contrib.auth.views` under `/accounts/`. (Implemented via `settings.INCLUDE_AUTH_URLS=False`, verified against the real installed `registration.backends.default.urls`/`registration.auth_urls` source (Django==1.7.7, django-registration-redux==1.2) rather than the originally-planned hand-filtered list — see note below.)
- [x] 2.2 Conditionally append `include('registration.backends.default.urls')` under `r'^accounts/'` only when `settings.ALLOW_SELF_REGISTRATION` is true, so registration is mounted in addition to (not instead of) the four views from 2.1. (Implemented via `settings.INCLUDE_REGISTER_URL=ALLOW_SELF_REGISTRATION`, django-registration-redux's own supported toggle, rather than a manual conditional include — same effect, lower risk.)
- [x] 2.3 Verify no urlpattern in the project resolves any of `password_reset`, `password_reset_done`, `password_reset_confirm`, `password_reset_complete` — confirm with `python manage.py show_urls` (or equivalent) or by requesting each path and checking for a 404, with `ALLOW_SELF_REGISTRATION` both true and false. (Verified with a standalone `resolve()`/`reverse()` check against the real pinned Django/django-registration-redux packages in an isolated container, both flag states: `/accounts/password/reset/` and `/accounts/password/reset/done/` 404, `reverse('auth_password_reset')` raises `NoReverseMatch`, in both states.)
- [ ] 2.4 Verify `login`, `logout`, and an authenticated `password_change` round-trip (change password, log out, log back in with the new password) all still work. (URL wiring confirmed live via the same isolated check — `auth_login`/`auth_logout`/`auth_password_change`/`auth_password_change_done` all resolve, using the exact view functions, regexes, and kwargs upstream's own `registration.auth_urls` uses. A live authenticated round-trip through the full nDialer stack — DB, sessions, real HTTP requests — was not run; that needs `docker compose up` against this repo, which is out of scope for this automated pass.)
- [x] 2.5 Verify `/accounts/register/` 404s with `ALLOW_SELF_REGISTRATION=false` (default) and resolves to the registration form with `ALLOW_SELF_REGISTRATION=true`. (Verified live in the same check: `reverse('registration_register')` raises `NoReverseMatch` when false, resolves to `/accounts/register/` when true.)

## 3. Templates

- [x] 3.1 In `newfies/templates/registration/login.html`, remove the `{% url 'auth_password_reset' %}` link (line 17) and replace it with static "Please email support" text (no link). (Also guarded the pre-existing "Not a member? Register" link on this page, and the equivalent one in `frontend/templates/frontend/master.html`, behind `{% if ALLOW_SELF_REGISTRATION %}` — see note below; without this, both pages would raise `NoReverseMatch` and 500 for every unauthenticated visitor once registration is gated off by default.)
- [x] 3.2 In `newfies/frontend/forms.py`, remove the dead `href="/password_reset/"` link (line 44) from `LoginForm`'s `FormActions`. (Also dropped the now-unused `HTML` import.)
- [ ] 3.3 Visually check the login page renders correctly with the new copy in place of the old link. (Not run live: `docker compose up web` needs a locally-built `newfies-freeswitch:v1.4` base image, compiled from source, which is out of scope for this pass. The template edit itself is a straightforward `{% if %}`/text swap; recommend a quick `docker compose up` check before shipping.)

## 4. Regression check

- [x] 4.1 Confirm Django admin's per-user "change password" action still works for a `user_profile`, `agent`, and `appointment` admin-registered user (all use stock `UserAdmin`), unaffected by this change. (Confirmed by construction: `git status` shows this change touches no file under `admin.py` anywhere in the project, and `r'^admin/'` is a separate, untouched include in `urls.py`.)
- [ ] 4.2 Run the existing test suite and confirm no test depends on `auth_password_reset` or the self-registration URLs being unconditionally present. (Not run: the project's test runner (`newfies/runtests.py`, django-nose/pytest-django) needs the full dependency stack installed, which this pass didn't build. `grep -rn "auth_password_reset" newfies/tests` and equivalent found no hits in a static search, but that's not a substitute for actually running the suite.)
